# تقرير مطابقة نصوص نظام العمل — الدفعة السابعة — المواد 151–175

## المرحلة

LABOR_LAW_TEXT_RECONCILIATION_BATCH_007_ARTICLES_151_175_WITH_AMENDMENT_POPUP_HANDLING

## الأساس

a15be82243a5562fe2da154f32860d6a51d50307

## الفرع

hermes/labor-law-text-reconciliation-batch-007-articles-151-175-popup-aware

## الملفات المنشأة

- `worksheets/labor_law/reconciliation_batches/labor_law_text_reconciliation_batch_007_articles_151_175.csv`
- `reports/labor_law/LABOR_LAW_TEXT_RECONCILIATION_BATCH_007_ARTICLES_151_175_REPORT.md`

## الملفات المعدلة

- `worksheets/labor_law/reconciliation_scaffold/article_inventory.csv`
- `worksheets/labor_law/reconciliation_scaffold/article_source_checklist.csv`
- `worksheets/labor_law/reconciliation_scaffold/extraction_quality_issues.csv`
- `worksheets/labor_law/reconciliation_scaffold/unresolved_issues_log.csv`
- `worksheets/labor_law/reconciliation_scaffold/readiness_summary.csv`

## المصدر الرسمي المستخدم

المصدر الرسمي العربي من البوابة الإلكترونية للأنظمة (BOE):
https://laws.boe.gov.sa/boelaws/laws/lawdetails/08381293-6388-48e2-8ad2-a9a700f2aa94/1

## المنهجية المتبعة للتعامل مع النوافذ المنبثقة

تمت مراجعة ملفات تتبع التعديلات قبل تعبئة أي صف. تم تحديد المواد المعدلة عبر `amendment_tracking.csv` و `m44_tracking.csv` ولم يتم تعليمها كجاهزة. لم يتم التقاط نص الـ DOM الرئيسي من BOE كنص رسمي حالي لها. لم يتم توليد أي نص قانوني مدمج من النص الأساس زائد نافذة التعديل المنبثقة.

## المواد المغطاة

المواد 151–175 (25 صفًا)

## عدد صفوف ملف الدفعة

25

## عدد المواد المطابقة بنجاح

19

## عدد المواد التي تحتاج مراجعة يدوية

6

## قائمة المواد المطابقة بنجاح

- المادة 153 (المادة الثالثة والخمسون بعد المائة)
- المادة 154 (المادة الرابعة والخمسون بعد المائة)
- المادة 157 (المادة السابعة والخمسون بعد المائة)
- المادة 158 (المادة الثامنة والخمسون بعد المائة)
- المادة 159 (المادة التاسعة والخمسون بعد المائة)
- المادة 161 (المادة الحادية والستون بعد المائة)
- المادة 162 (المادة الثانية والستون بعد المائة)
- المادة 163 (المادة الثالثة والستون بعد المائة)
- المادة 164 (المادة الرابعة والستون بعد المائة)
- المادة 165 (المادة الخامسة والستون بعد المائة)
- المادة 166 (المادة السادسة والستون بعد المائة)
- المادة 167 (المادة السابعة والستون بعد المائة)
- المادة 169 (المادة التاسعة والستون بعد المائة)
- المادة 170 (المادة السبعون بعد المائة)
- المادة 171 (المادة الحادية والسبعون بعد المائة)
- المادة 172 (المادة الثانية والسبعون بعد المائة)
- المادة 173 (المادة الثالثة والسبعون بعد المائة)
- المادة 174 (المادة الرابعة والسبعون بعد المائة)
- المادة 175 (المادة الخامسة والسبعون بعد المائة)

## قائمة المواد المعدلة/المنبثقة/الملغاة/المراجعة اليدوية

- المادة 151 (معدلة؛ م/44 و5/6/1436؛ تستوجب مصالحة النافذة المنبثقة؛ مرتبطة بم/44؛ issue_094)
- المادة 152 (معدلة؛ 5/6/1436؛ تستوجب مصالحة النافذة المنبثقة؛ issue_095)
- المادة 155 (معدلة؛ م/134؛ تستوجب مصالحة النافذة المنبثقة؛ issue_096)
- المادة 156 (ملغاة/معطلة بتعديل عبر م/134؛ المشكلة القائمة issue_017 منقولة؛ لم تُضف مشكلة جديدة)
- المادة 160 (معدلة؛ 5/6/1436؛ تستوجب مصالحة النافذة المنبثقة؛ issue_097)
- المادة 168 (معدلة؛ م/44؛ تستوجب مصالحة النافذة المنبثقة؛ مرتبطة بم/44؛ issue_098)

## معالجة المواد الخاصة

- **المادة 156**: ملغاة/معطلة بتعديل عبر م/134 (مؤكدة في `amendment_tracking.csv` و `mukarrar_deleted_renumbered_tracking.csv`). تم نقل المشكلة القائمة `issue_017` ولم تُضف مشكلة جديدة. لم يتم التقاط النص الأساس/القديم كنص رسمي حالي. عُلمت بـ `DO_NOT_INGEST_YET` / `needs_manual_review`.
- **المادة 151**: معدلة بم/44 و5/6/1436 (مرجعان للتعديل في `amendment_tracking.csv`). مرتبطة بم/44 وفق `m44_tracking.csv`. أُضيفت المشكلة الجديدة `issue_094`.
- **المادة 168**: معدلة بم/44. مرتبطة بم/44 وفق `m44_tracking.csv`. أُضيفت المشكلة الجديدة `issue_098`.

## ملخص مقارنة المرشحين

لم يتوفر مرشح مرفوع للمقارنة في هذه الدفعة. جميع الصفوف النظيفة استخدمت `BOE_DOM_ARTICLE_TEXT` كأسلوب مصدر مع `uploaded_candidate_compared_flag=not_available`.

## ملخص تحديث article_inventory.csv

تم تحديث 25 صفًا للمواد 151–175:
- 19 صفًا تحديث إلى `OFFICIAL_TEXT_CAPTURED_BATCH` / `TEXT_RECONCILED_BATCH_007` / `unresolved_issue_flag=no`
- 5 صفوف معدلة تحديث إلى `NEEDS_MANUAL_CAPTURE` / `DO_NOT_INGEST` / `unresolved_issue_flag=yes`
- 1 صف ملغي تحديث إلى `NEEDS_MANUAL_CAPTURE` / `DO_NOT_INGEST` / `unresolved_issue_flag=yes`

## ملخص تحديث article_source_checklist.csv

تم تحديث 25 صفًا للمواد 151–175:
- 19 صفًا نظيفًا تحديث إلى `ARTICLE_TEXT_CAPTURED_FROM_BOE` / `OFFICIAL_TEXT_CAPTURED_BATCH`
- 5 صفوف معدلة تحديث إلى `SOURCE_PAGE_IDENTIFIED` / `NEEDS_MANUAL_CAPTURE`
- 1 صف ملغي تحديث إلى `SOURCE_PAGE_IDENTIFIED` / `NEEDS_MANUAL_CAPTURE`

## ملخص تحديث extraction_quality_issues.csv

أُضيفت 5 مدخلات جديدة:
- `eq_batch007_art151` — المادة 151 معدلة؛ تستوجب مصالحة النافذة المنبثقة
- `eq_batch007_art152` — المادة 152 معدلة؛ تستوجب مصالحة النافذة المنبثقة
- `eq_batch007_art155` — المادة 155 معدلة؛ تستوجب مصالحة النافذة المنبثقة
- `eq_batch007_art160` — المادة 160 معدلة؛ تستوجب مصالحة النافذة المنبثقة
- `eq_batch007_art168` — المادة 168 معدلة؛ تستوجب مصالحة النافذة المنبثقة

## ملخص تحديث unresolved_issues_log.csv

أُضيفت 5 مشكلات جديدة (issue_094 إلى issue_098):
- `issue_094` — المادة 151 معدلة (م/44 و5/6/1436)؛ تستوجب مصالحة النافذة المنبثقة؛ مرتبطة بم/44
- `issue_095` — المادة 152 معدلة (5/6/1436)؛ تستوجب مصالحة النافذة المنبثقة
- `issue_096` — المادة 155 معدلة (م/134)؛ تستوجب مصالحة النافذة المنبثقة
- `issue_097` — المادة 160 معدلة (5/6/1436)؛ تستوجب مصالحة النافذة المنبثقة
- `issue_098` — المادة 168 معدلة (م/44)؛ تستوجب مصالحة النافذة المنبثقة؛ مرتبطة بم/44

المادة 156 (ملغاة/معطلة) — المشكلة القائمة `issue_017` منقولة ولم تُضف مشكلة جديدة.

إجمالي صفوف البيانات في `unresolved_issues_log.csv`: 98 (كان 93، زاد بـ 5).

## نتيجة readiness_summary.csv

- `ingestion_readiness_decision`: NOT_READY
- `total_unresolved_issues`: 98
- `summary_notes`: الدفعات 001–007 معبأة. الدفعة 007 تغطي المواد 151–175 (25 صفًا): 19 مطابقة بنجاح، 6 تحتاج مراجعة يدوية. لا استيعاب نهائي.

## التحقق الصريح من العدد غير المحلول

- العدد السابق (الحد الأدنى غير المحلول): 93
- العدد الحالي لصفوف البيانات في `unresolved_issues_log.csv`: 98
- العدد الحالي لـ `total_unresolved_issues` في `readiness_summary.csv`: 98
- زاد العدد من 93 إلى 98 (زيادة 5) بسبب 5 مشكلات جديدة لمصالحة النوافذ المنبثقة للمواد المعدلة (issue_094 إلى issue_098).
- لم توثق أي إغلاقات؛ لم يحدث انخفاض في العدد.
- التأكيد: لم ينخفض العدد غير المحلول دون 93.

## ما لم يتم عن قصد

- لم يتم إنشاء سجلات نهائية لنظام العمل.
- لم يتم تعديل السجل أو التصدير أو وقت التشغيل أو المدققات.
- لم يتم إنشاء سجلات إنجليزية أو مطابقة ثنائية/ثلاثية اللغة.
- لم يتم توليد نص قانوني مدمج من النص الأساس زائد نافذة التعديل المنبثقة.
- لم تُضف أي ملفات RAG أو واجهة أو API أو شبكة أو LLM أو تضمين.
- لم يتم الالتزام بأي ملفات مصدر أو PDF أو HTML لـ BOE أو JSON أو JSONL أو XLSX.
- لم يتم تعديل ملفات نظام الشركات.

## تأكيد عدم حدوث استيعاب نهائي

لم يحدث استيعاب نهائي. جميع المواد المعدلة والملغاة معلمة بـ `DO_NOT_INGEST_YET`. المواد النظيفة معلمة بـ `ready_for_future_ingestion_flag=yes` لكن لم يتم استيعابها.

## تأكيد عدم تغيير السجل/التصدير/وقت التشغيل/المدققات

لم يتم تعديل أي ملفات سجل أو تصدير أو وقت تشغيل أو مدققات. تم إنشاء أو تحديث ملفات ورقة العمل والتقارير فقط.

## تأكيد عدم إنشاء سجلات إنجليزية أو مطابقة

لم يتم إنشاء سجلات إنجليزية. لم يتم إجراء مطابقة ثنائية أو ثلاثية اللغة. الإنجليزية تبقى مرجعًا فقط.

## تأكيد عدم وجود ملفات ممنوعة

لم يتم الالتزام بأي ملفات مصدر أو PDF أو HTML لـ BOE أو ملفات TXT/PDF مرفوعة أو JSON أو JSONL أو XLSX أو ملفات PDF مولدة.

## تأكيد عدم توليد نص قانوني مدمج

لم يتم توليد نص قانوني مدمج من النص الأساس زائد نافذة التعديل المنبثقة. حقول `official_arabic_text_reconciled` فارغة للمواد المعدلة.

## نتائج التحقق الفعلية

### py_compile

```
python -m py_compile tools/check_labor_law_reconciliation_batch.py
```

النتيجة: PASS — (exit code 0)

### مدقق الدفعة

```
python tools/check_labor_law_reconciliation_batch.py --batch 007 --range 151-175 --unresolved-floor 93
```

النتيجة: PASS — جميع الفحوص اجتازت
- بنية ملف CSV للدفعة: OK
- تعدادات الجاهزية/غير المحلولة: OK
- بنية التقرير وصيغ الحدود: OK

### make validate

```
make validate
```

النتيجة: PASS — `RESULT: ALL CHECKS PASSED ✓` (exit code 0)

### make test

```
make test
```

النتيجة: 9 failed, 2483 passed — جميع حالات الفشل التسع هي حالات فشل أساسية معروفة في `chinese_remediation`؛ لم يتم إدخال أي فشل جديد. تم استعادة ملفات البيانات الصينية بعد تشغيل الاختبار.

حالات الفشل الأساسية المعروفة: 9 فشول في `chinese_remediation`. لم يتم إدخال أي فشل جديد. تم استعادة ملفات البيانات الصينية بعد تشغيل الاختبار.

## الحدود القانونية والمنتجية

المصدر العربي الرسمي يحكم والإنجليزية تبقى مرجعًا فقط. هذه المرحلة تنفذ مطابقة نصوص عربية على مستوى ورقة العمل فقط. لا تنشئ سجلات نهائية للنظام، ولا تتحقق من الصحة القانونية، ولا تقدم استشارة قانونية. repository-owner legal review active; external legal review optional for enterprise/official adoption

## المرحلة التالية الموصى بها

LABOR_LAW_TEXT_RECONCILIATION_BATCH_008_ARTICLES_176_200_WITH_AMENDMENT_POPUP_HANDLING